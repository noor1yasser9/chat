class UserData {
  String uid;
  String name;
  String email;
  String image;
  String token;
  bool isOnline;

  UserData(
      this.uid, this.name, this.email, this.image, this.isOnline, this.token);
}
